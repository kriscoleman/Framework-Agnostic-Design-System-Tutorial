import type { GatsbyConfig } from 'gatsby';

const config: GatsbyConfig = {
  siteMetadata: {
    title: 'Gatsby App - Framework Agnostic Demo',
    siteUrl: 'https://example.com',
  },
  graphqlTypegen: true,
  plugins: [],
};

export default config;
